$kind: http-request
name: "F14: Logout (sesijas beigas)"
url: "{{base_url}}/api/auth/logout"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('HTTP 204 No Content', () => pm.response.to.have.status(204));
    language: text/javascript
order: 9000
